#include <stdio.h>
/**
  *main - entry point
  *return: always 0 (success)
  */
int main(void)
{
	//this program returns 0 to indicate a successful execution
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
